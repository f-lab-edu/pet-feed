plugins {
    id 'base-spring.core'
}

dependencies {
    api project(':common-domain')

    // JWT
    api 'io.jsonwebtoken:jjwt-api:0.12.6'
    api 'io.jsonwebtoken:jjwt-impl:0.12.6'
    api 'io.jsonwebtoken:jjwt-jackson:0.12.6'

    // 나중에 필요한 인프라 의존성 추가
    // Redis, S3, Kafka 등은 필요시 추가
}
